{
  "name": "cfb-graphics-generator",
  "version": "1.0.0",
  "description": "Automated graphic generator for College Football content",
  "type": "module",
  "scripts": {
    "generate": "node scripts/generate-graphic.mjs",
    "install-deps": "npm install"
  },
  "dependencies": {
    "playwright": "^1.57.0",
    "tailwindcss": "^3.3.0"
  },
  "devDependencies": {
    "@tailwindcss/cli": "^0.1.2"
  }
}
